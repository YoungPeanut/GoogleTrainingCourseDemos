package info.ipeanut.googletrainingcoursedemos.customviews.imageview;

/**
 * Canvas提供了ClipPath, ClipRect, ClipRegion 等方法来裁剪，通过Path, Rect ,Region 的不同组合，Android几乎可以支持任意形状的裁剪区域。
 *
 * Created by chenshao on 16/11/16.
 */
public class CircleImageView2 {


//    int radius = src.getWidth() / 2;　//src为我们要画上去的图，跟上一个示例中的bitmap一样。
//    Bitmap dest = Bitmap.createBitmap(src.getWidth(), src.getHeight(), Bitmap.Config.ARGB_8888);
//    Canvas c = new Canvas(dest);
//    Paint paint = new Paint();
//    paint.setColor(Color.BLACK);
//    paint.setAntiAlias(true);
//    Path path = new Path();
//    path.addCircle(radius, radius, radius, Path.Direction.CW);
//    c.clipPath(path);   //裁剪区域
//    c.drawBitmap(src, 0, 0, paint);　　//把图画上去

}
